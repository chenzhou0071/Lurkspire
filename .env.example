# 复制为 .env 并修改（docker compose 自动读取；本文件不提交）
LURKSPIRE_DB_PASS=change-me
# Dockerfile 选择：
#   默认（留空/不设）= Dockerfile（多阶段自构建——需要能拉 golang 镜像）
#   拉不到 golang 镜像时 = Dockerfile.prebuilt（本地交叉编译后打包，见文件内注释）
# GATEWAY_DOCKERFILE=Dockerfile.prebuilt
